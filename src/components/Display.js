function Display({name, title}) {

    return (
            <div>
                <p className='name'>{ name }</p>
                <p className='title'>{ title }</p>
            </div>
    );
};


export default Display;